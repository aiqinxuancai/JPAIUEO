<UserControl x:Class="JPAIUEO.View.WindowMainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JPAIUEO.View"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="208">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel >
                <StackPanel >
                    <Label Content="音类型设置"></Label>
                    <StackPanel Orientation="Horizontal" Margin="5,0" >
                        <CheckBox x:Name="checkBoxQing" Content="清音" IsChecked="{Binding OpenQingYin}"  />
                        <CheckBox x:Name="checkBoxZhuo" Content="浊音" IsChecked="{Binding OpenZhuoYin}" Margin="5,0,0,0" />
                        <CheckBox x:Name="checkBoxBanZhuo" Content="半浊音" IsChecked="{Binding OpenBanZhuoYin}" Margin="5,0,0,0" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Margin="0,5,0,0" >
                    <Button x:Name="buttonExit" Content="退出" Margin="6,0" Click="buttonExit_Click" ></Button>
                </StackPanel>

            </StackPanel>

        </ScrollViewer>

    </Grid>
</UserControl>
